package ISA.Team54.rating.mapper;

import ISA.Team54.rating.dto.RatingRequestDTO;
import ISA.Team54.rating.model.Rating;

public class RatingMapper {
    public Rating RatingToRatingDTO(Rating rating){
        return new Rating(

        );
    }
}
